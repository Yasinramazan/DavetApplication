/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['blog.etasimacilik.com','www.trthaber.com'], // Domaini buraya ekleyin
      },
};



export default nextConfig;
